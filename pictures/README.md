# Custom pictures

Drop an image here and name it in `kg_content.py` where an emoji would go:

    ("Y", "Yak", "yak.png"),

Why this exists: some words a child is actually taught have no emoji at all, and
the nearest one is the wrong animal or the wrong object. On a chart a child is
learning from, that is worse than no picture.

Still needing one (they show the letter alone until a file is added):

| entry            | file to add     |
|------------------|-----------------|
| Y for Yak        | `yak.png`       |
| अ से अनार        | `anar.png`      |
| इ से इमली        | `imli.png`      |
| ई से ईख          | `eekh.png`      |
| ओ से ओखली        | `okhli.png`     |
| ल से लट्टू        | `lattu.png`     |
| ष से षट्कोण      | `shatkon.png`   |

Any size; PNG with a transparent background looks best. The image is fitted into
a square and centred, so proportions are kept rather than stretched. A missing
file is not an error -- the screen just shows the letter on its own.
